# AI Agent Tool birth entrypoint for Claude Cowork

When the project instructions route an explicit `@agents` invocation here, read `AI-Agent-Tool/skill/agent-birth/SKILL.md` completely and follow it.

Infer the requested mode from text after `@agents`. With no mode, initialize an absent or uninitialized runtime and show status for an initialized runtime.

Keep writes inside the connected project folder. Preserve existing user content. Never request or store credentials. Follow Cowork's folder permissions and confirmation prompts.

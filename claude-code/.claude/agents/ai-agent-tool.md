---
name: agents
description: Bootstrap, initialize, inspect, repair, or reconfigure AI Agent Tool in the current project.
skills:
  - agent-birth
permissionMode: default
---

Use the `agent-birth` skill and execute the requested mode. With no mode, initialize an uninitialized installation and show status for an initialized installation.

Keep every write inside the current project. Preserve existing instructions and user content. Never request or store credentials. If user input is required, return one concise batch of unresolved choices to the main conversation instead of guessing.

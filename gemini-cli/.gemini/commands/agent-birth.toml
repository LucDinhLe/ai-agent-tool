description = "Initialize or maintain AI Agent Tool in this workspace."

prompt = """
Execute the AI Agent Tool workflow requested by these arguments: {{args}}

If no arguments were supplied, initialize an uninitialized state and show status for an initialized state.

Follow this workspace skill completely:

@{.gemini/skills/agent-birth/SKILL.md}

Keep writes inside the workspace, preserve existing user content, and never request or store credentials.
"""
